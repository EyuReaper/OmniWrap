{
  "name": "omniwrap",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "cd apps/web && npm run dev",
    "build": "cd apps/web && npm run build",
    "start": "cd apps/web && npm run start"
  },
  "devDependencies": {
    "@prisma/config": "^7.2.0",
    "@types/node": "^25.0.3",
    "dotenv": "^17.2.3",
    "prisma": "^7.2.0",
    "typescript": "^5"
  }
}
